# Handoffs

Active handoff briefs between AI models and sessions. See `00-system/handoff-protocol.md` for the brief format and routing rules.

When work is complete and the completion report is processed, set the handoff's status to `complete`.
