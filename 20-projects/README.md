# 20-projects

Active projects with their own internal structure. Each project directory mirrors the complexity of the project itself.

Current projects:
- `rmm/` — Run Mad Maps (primary project, ~97 notes, migrated in sub-domain batches)

Future projects get their own subdirectory here when they reach operational scale.
